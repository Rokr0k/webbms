<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WebBMS</title>
</head>

<body>
    <h1>Web BMS Streaming</h1>
    <ul>
        <% for(i of Object.keys(bms)) { %>
            <li>
                <a href="<%= i %>">
                    <% if(bms[i].subtitle) { %>
                        <%= `[${bms[i].playlevel}] ${bms[i].artist} - ${bms[i].title} ${bms[i].subtitle}` %>
                            <% } else { %>
                                <%= `[${bms[i].playlevel}] ${bms[i].artist} - ${bms[i].title}` %>
                                    <% } %>
                </a>
            </li>
            <% } %>
    </ul>
</body>

</html>